{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.11.1",
    "hardhat-abi-exporter": "^2.10.0"
  },
  "scripts": {
    "uniswap-ganache": "npx hardhat run scripts/deploy.ts --network ganache",
    "uniswap-local": "npx hardhat run scripts/deploy.ts",
    "console-ganache": "npx hardhat console --network ganache",
    "console-local": "npx hardhat console",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
